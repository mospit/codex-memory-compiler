# Claude Hook Compatibility (Optional)

This folder isolates legacy Claude lifecycle hooks from the core Codex-first workflow.

- `session-start.py`
- `session-end.py`
- `pre-compact.py`

These scripts are retained as optional compatibility scaffolding only.
The primary supported workflow for this repository is Codex app + manual/scripted ingest.
